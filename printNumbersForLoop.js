// using a while loop and the other using a for loop.


function printNumbersForLoop(num) {
    for (let count = 0; count <= num; count++) {
        console.log(count);
    }
}    

printNumbersForLoop(10);